package edu.gae.web.rest;

import java.io.IOException;

import org.junit.Test;

public class ClienteRestTest {

	@Test
    public void shouldCheckURIs() throws IOException {       
//		URI uri = UriBuilder.fromUri("http://localhost/").port(8282).build();	
//		 
//		// Create an HTTP server listening at port 8282
//		HttpServer server = HttpServer.create(new InetSocketAddress(uri.getPort()), 0);
//		// Create a handler wrapping the JAX-RS application
//		HttpHandler handler = RuntimeDelegate.getInstance().createEndpoint(new ApplicationConfig(), HttpHandler.class);
//		// Map JAX-RS handler to the server root		
//		server.createContext(uri.getPath(), handler);		
//		// Start the server
//		server.start();
		
//		ClientConfig config = new DefaultClientConfig();			
//		
//		String content = "{\"nome\":\"Machado\"}";
//		
//		Client client = Client.create(config);
//		WebResource webResource = client.resource("http://localhost:8282/cliente/create");
//		ClientResponse response = webResource.type(MediaType.APPLICATION_JSON_TYPE).post(ClientResponse.class,content);	
//        System.out.println(response);
//		assertEquals(200, response.getStatus());
	}		
	

	
}
